# Copyright 2026 — Aurel & Co. đồ án clock
# SPDX-License-Identifier: MIT
#
# Cài phần AI Agent: pip install -r requirements.txt trong venv.
# Năm package của commerce-agents cài editable từ vendor/ (đã pin nội bộ,
# không resolve từ index công cộng — đúng kiểu upstream requirements.txt).

# --- vendored packages (commerce-agents, Apache-2.0) ---
-e ./vendor/commerce-common
-e ./vendor/shopping-agent/core
-e ./vendor/shopping-agent/runtime-messages-api
-e ./vendor/merchant-agent/core
-e ./vendor/merchant-agent/runtime-messages-api

# --- runtime deps của aurel_agents ---
httpx>=0.28
fastapi>=0.115
uvicorn>=0.30
sse-starlette>=2.1
python-dotenv>=1.0

# --- transitive deps của vendor packages (không có [examples] extras) ---
# (pin của upstream phần nào chưa có trên index — dùng range tương thích)
annotated-doc>=0.0.5
annotated-types>=0.8
anthropic>=0.91
attrs>=23.0
distro>=1.9
docstring-parser>=0.18
h11>=0.16
httpcore>=1.0
idna>=3.7
jiter>=0.5
pydantic>=2.7
pydantic-settings>=2.4
pyjwt>=2.9
pyyaml>=6.0
sniffio>=1.3
starlette>=0.40
typing-extensions>=4.12
typing-inspection>=0.4

# --- dev/test ---
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.22
ruff>=0.6
